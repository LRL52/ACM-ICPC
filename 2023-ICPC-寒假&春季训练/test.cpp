/*** 
 * @Author: LRL52
 * @Date: 2023-05-27 13:25:04
 * @LastEditTime: 2023-06-09 20:32:35
 */
#include <algorithm>
#include <bits/stdc++.h>
#include <vector>
#define rep(i, a, b) for(int i = (a); i <= (b); ++i)
using namespace std;
using i64 = long long;
using pii = pair<int, int>;
const int N = 1e3 + 55, inf = 0x3f3f3f3f, MOD = 1e9 + 7;

signed main() {
#ifdef LRL52
	freopen("P8791.in", "r", stdin);
#endif
	ios::sync_with_stdio(false); cin.tie(nullptr); cout.tie(nullptr);
    char s[105];
    fgets(s, 104, stdin);
    fgets(s, 104, stdin);
	// vector<int> s{1, 2, 3};
    // auto it = lower_bound(s.begin(), s.end(), 2);
    // cout << *it << endl;
    // for (int i = 1; i <= 10; ++i) {   
    //     it = prev(it);
    //     cout << *it << endl;
    // }
	return 0;
}