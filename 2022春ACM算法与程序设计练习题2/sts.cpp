#include <cstdio>
#include <cstdlib>
#include <cstring>
#include <algorithm>
using namespace std;
const int N = 1055, M = 2055;

int main() {
	freopen("read.in", "r", stdin);
	int n = 0, x = 0;
	scanf("%da%d", &n, &x);
	printf("%d %d\n", n, x);
	return 0;
}